# 正式角色计划（原创角色替换）

> 状态：**准备中，等待设计输入**（2026-09-12 立项）
> 目标：用**原创角色**替换当前内置的「梅尔」（`char_03`，外部作品角色素材，存在版权风险），作为正式版的陪伴角色。
> 相关：[角色资源规范](../assets/chars/README.md)、[开发文档](开发文档.md) §7、[发布与打包](发布与打包.md)。

## 1. 背景与目标

- 现状：`char_03` 梅尔的立绘 / 像素帧 / 人设来自外部作品（见 `persona.json` 的 `origin`），适合开发与预览，但不适合作为正式发布素材
- 目标：1 名**原创正式角色**（人设 + 完整素材 + 互动），进入正式发布包，作为默认陪伴角色
- 同时处理：把旧素材从公共仓库 / 发布包中移除；是否替换占位角色「小灯」（`char_01`）待定

## 2. 现有角色盘点

| 目录 | 角色 | 状态 | 处理建议 |
|---|---|---|---|
| `char_02` | （空，已预留） | 仅 `.gitkeep` | 建议作为原创正式角色的目录（id 可另起） |
| `char_01` | 小灯 | 临时占位（基础素材，无差分 / 互动） | 待定：保留占位 / 升级为正式 / 移除 |
| `char_03` | 梅尔 | 完整（外部素材） | 替换完成后从仓库与发布包移除 |
| `_template` | （空模板） | 仅 `.gitkeep` | 可用于新角色目录起步 |

## 3. 待确认的设计输入（需要你提供 / 确认）

- [ ] 名字：中文名 / 全名 / 玩家可见的显示名
- [ ] 世界观：种族 / 来历（原创设定，避免与现有作品雷同）
- [ ] 外观与配色：发色 / 瞳色 / 服装 / 体型
- [ ] 性格与说话风格：语气、口头禅、句尾、对玩家的称呼、禁区
- [ ] 相处起点：好感 Lv1「朋友」阶段的语气与距离感
- [ ] 互动菜单 3 条（点击角色）+ 家具互动（书架 / 盆栽等）
- [ ] 差分表情档位（默认 6 档：默认 / 开心 / 低落 / 害羞 / 走神 / 惊讶）
- [ ] 素材来源：你绘制 / 外部生成后导入 / 需要先出设计稿与规格拆分？
- [ ] 角色目录 id（建议 `char_02`）与列表排序 `order`
- [ ] 是否保留「小灯」；下一版版本号（建议 v0.2.0）

## 4. 素材清单（按角色资源规范）

- [ ] `persona.json`（`id` / `name` / `origin` / `appearance` / `personality` / `greeting` / `speech_style` / `system_prompt` 等）
- [ ] `portrait.png`（立绘原图，原分辨率，越界不裁）
- [ ] `portraits/`：`default` / `happy` / `low` / `shy` / `distracted` / `surprised`（可选 `angry` / `blushing_worried`）
- [ ] `sprites/`：`idle_0~1`、`walk_0~3`（128×128、透明背景、像素化）；可选 `sit` / `sleep` / `talk` / `happy` / `sad`
- [ ] `anim_map.json`（各动画帧数 / 帧率 / 循环）
- [ ] `interactions.json`（互动名 / 入口 / 动作 / 表情 / 台词 / 效果 / 好感 / 次数限制）

## 5. 代码接入清单（替换时逐项检查）

> 推荐先用 `git grep -n "char_03\|梅尔"` 全仓库扫描，再按下表逐项确认。

- [ ] 默认角色常量：`GameManager.DEFAULT_CHAR_ID`、`StatusManager.DEFAULT_CHAR_ID`、`char_controller.DEFAULT_CHAR_ID`（当前均为 `"char_03"`）
- [ ] fallback 常量 / 默认文案：`chat_screen.gd`、`diary_screen.gd`、`phone_overlay.gd`、`portrait_manager.gd`、`blackjack_game.gd`、`snake_game.gd`、`help_overlay.gd`、`notification_toast.gd`
- [ ] 场景默认文本：`chat_screen.tscn`（标题 / 输入提示）、`diary_screen.tscn`（标题）、`phone_overlay.tscn`（标题）、`notification_toast.tscn`（横幅）（运行时多为动态名覆盖，但默认值需同步）
- [x] `scenes/main.tscn`：2026-09-13 已去掉对角色的硬引用（不再 preload `char_03/portrait.png`，`NameLabel` 默认文案置空）——立绘 / 名字均由 `portrait_manager` / `GameMain._apply_character` 运行时加载，替换角色无需再改场景
- [ ] 死数据清理：`data/config.json`（`active_char`）、`data/memory.json`（`char_03` 分区）——当前无代码引用，替换时可一并删除
- [ ] 离线结算 / 聊天 / 手机 / 日记的人设注入（按 char_id 读取）
- [ ] 角色选择排序（`order`）与新档默认角色
- [ ] 旧存档：`user://game.json` 中 `current_char_id=char_03` 不可用时回退默认角色（逻辑已存在，需实测）
- [ ] 移除 `char_03` 后全链路回归：角色切换 / 聊天 / 手机 / 日记 / 立绘 / 互动 / 好感

## 6. 版权清理清单

> 现状（2026-09-13）：用户决定**暂缓执行**（暂无原创替代素材），素材暂留并在 `THIRD_PARTY_NOTICES.md` 标注；替换角色完成后按本清单执行。
> **完成定义**：仓库历史与工作树不含 `char_03` 素材；导出 pck 扫描无 `char_03` / `mea`；README 与 `THIRD_PARTY_NOTICES.md` 已更新；v0.1.0 Release 附件已下架或替换。

- [ ] `char_03` 目录（含 `mea原图.png` / `Mea人设.md` / `portraits/` / `sprites/`）从公共仓库移除
- [ ] **重写 git 历史**（当前提交中仍有素材，仅删文件不够）：`git clone --mirror` 备份 → `git filter-repo --path assets/chars/char_03 --invert-paths` → 强推；随后手动删除 GitHub Release v0.1.0 附件（历史重写不会删附件）
- [ ] 文档与代码中的「梅尔」引用改为新角色名或动态描述（用 `git grep "梅尔"` 核对）
- [ ] 重新导出后扫描发布包，确认不含 `char_03` / `mea`（可脚本化）
- [ ] 更新 README / `THIRD_PARTY_NOTICES.md` / 发布说明（移除「未授权」标注）

## 7. 建议流程

1. 确认第 3 节设计输入
2. 制作 / 导入素材（第 4 节清单）
3. 放入 `assets/chars/<id>/`，在游戏内验证自动发现
4. 修改默认常量与 fallback（第 5 节）
5. `--headless --import` + 冒烟测试（见 `开发文档.md` §10）
6. 全链路实测（切换 / 聊天 / 手机 / 日记 / 立绘 / 互动 / 旧存档）
7. 移除 `char_03` 素材与引用
8. 重新打包 → 发布 v0.2.0（Release 说明注明「角色素材已替换」）

## 8. 验证清单（完成定义）

- [ ] 角色出现在切换列表，排序正确，可原地切换
- [ ] 立绘 / 差分表情 / 像素动画正常，缺档时降级正常
- [ ] 互动菜单与家具互动正常，数值 / 上限 / 冷却生效
- [ ] 聊天 / 手机 / 日记 / 离线结算使用新角色人设与名字
- [ ] 旧存档（含 char_03）可正常进入，无报错
- [ ] 发布包不含旧角色素材，冒烟通过