# AIHome 导出打包说明（2026-09-05）

> 目标：把 Godot 项目打包成可分发的 Windows 版本（.exe），并上传 GitHub Release。
> 当前状态：**已完成「资源包打包」预览与导出配置；独立 .exe 需先安装 Godot 导出模板（约 500MB，需联网）。**

---

## 1. 现在能看到什么（已完成）

- ✅ 项目已生成资源包：`build/AIHome.pck`（约 3.7MB，`godot --headless --export-pack` 生成，**不需要导出模板**）。
- ✅ 导出配置已提交：`export_presets.cfg`（Windows Desktop x86_64，**`embed_pck=false` 分离结构**，输出 `build/AIHome-win64/AIHome.exe` + `AIHome.pck` + 外部 `assets/bgm/`）。
- ✅ 用 Godot 编辑器二进制直接运行 pck 的「预览窗口」已可启动：

```bash
# 效果与正式版完全一致（画面/交互），只是宿主是编辑器二进制而非独立 exe
"C:\Users\JOYCEPC\Godot\Godot_v4.7.2-stable_win64.exe" --main-pack "D:\githb\ai-home\build\AIHome.pck"
```

> 该窗口直接弹出即可查看「打包后的游戏长什么样」；关掉窗口即可（进程名为 Godot_v4.7.2）。

## 2. 为什么还没有独立 .exe

Godot 独立导出（`--export-release`）需要**导出模板**（platform export templates）：
- Windows 模板包约 **500MB**（`Godot_v4.7.2-stable_export_templates.tpz`）；
- 当前开发沙箱无外网，无法代下；装好模板后一条命令即可导出。

## 3. 正式打包步骤（5 分钟）

### 3.1 安装导出模板（一次性，本机有网即可）

方式 A（推荐，图形界面）：
1. 打开 Godot 编辑器 → 菜单 `Editor` → `Manage Export Templates…` → `Download and Install`；
2. 等待下载完成（也可先下载 `Godot_v4.7.2-stable_export_templates.tpz` 再 `Install from File`）。

方式 B（命令行）：下载 `.tpz` 后解压到 `%APPDATA%\Godot\export_templates\4.7.2.stable\`。

### 3.2 导出

```bash
# 项目根目录（D:\githb\ai-home）
"C:\Users\JOYCEPC\Godot\Godot_v4.7.2-stable_win64_console.exe" \
  --headless --export-release "Windows Desktop" \
  "D:\githb\ai-home\build\AIHome-win64\AIHome.exe" --path "D:\githb\ai-home"
```

- 产物：`build/AIHome-win64/` 下 `AIHome.exe` + `AIHome.pck`（**分离结构**：更新内容只需替换 `AIHome.pck`，README/角色素材也可直接替换 `assets/` 目录文件）。
- 玩家放音乐：把 `.wav/.ogg/.mp3/.flac` 丢进游戏目录的 `assets/bgm/`（与 exe 同级，解压即见），重开游戏生效。

### 3.3 分发 / GitHub Release

```bash
cd build
zip -r AIHome-win64.zip AIHome-win64   # 或直接压缩 AIHome-win64 文件夹
```

在 GitHub Releases 新建 `v0.1.0-alpha（或 v0.1.0）`，上传：
- `AIHome-win64.zip`（Windows 玩家下载即玩：解压 → 双击 AIHome.exe）
- 可选附加 README.txt（玩法简介 / 设置 API Key 位置 / 免责声明：内置 API Key 需玩家自填才能聊天）

## 4. 打包产物结构（示意）

```
AIHome-win64/
├── AIHome.exe            # 可执行文件（引擎；约 109MB）
├── AIHome.pck            # 游戏资源包（更新只需替换它）
└── assets/
    └── bgm/              # 玩家自备 BGM（放 wav/ogg/mp3/flac）
        └── README.md
```

```
build/
├── AIHome.pck            # 资源包（调试用）
└── AIHome-win64/         # 正式发布目录（exe + pck + assets）
```

## 5. 注意事项

- `user://` 数据（config/memory/game/status/room_layout）存在 `%APPDATA%\Godot\app_userdata\AIHome`，与运行器无关，打包后玩家首次运行自动创建。
- **BGM 已改为外部目录**：发布版扫描 `assets/bgm/`（exe 同级）→ 开发版 `res://assets/bgm` → `user://bgm` 兜底；`assets/bgm` 已在导出时排除出 pck。
- API Key 由玩家在「设置 → AI 设置」中自填；仓库与打包产物均不含 Key。
- 发布前建议：换正式应用图标（`project.godot` 的 `config/icon` 与 exe `application/icon`）、写版权信息与玩法说明。
