# AIHome 交接文档

> 这是给下一个对话/开发者接手用的文档。目标是以最快速度了解项目现状、约定和下一步。

## 1. 项目概览

- 名称：AIHome / AI-Character-home
- 引擎：Godot 4.7.2（GDScript）
- 平台：PC（当前 Windows 开发，Godot 项目本身跨平台）
- 类型：像素风桌面陪伴 + AI 对话 + 小游戏 + AI 自主生活
- 仓库：Git 仓库（master），工作区干净，最新提交见 `git log --oneline`
- 本机 Godot：`C:\Users\JOYCEPC\Godot\Godot_v4.7.2-stable_win64_console.exe`

## 2. 当前完成状态（P0–P3 已完成，2026-08-30；P4 已启动——多角色切换已完成 2026-09-04）

### P3 完成总览
- **Graphwar 仿·函数对打（多球版）**：双方 3 球、直接输函数、起点发射、拖尾轨迹、随机球形障碍、穿透弹、黑底可滚动教程
- **井字棋**：3×3 与 AI 对战（AI 会堵会赢，非完美博弈，玩家可赢）
- **立绘差分 6 档**：状态阈值（开心/低落）+ 事件触发（惊讶/害羞/走神等）
- **其他互动 5 条（角色文件数据驱动）**：摸摸头/投喂零食/玩小玩具/一起看书/浇花，定义在 `assets/chars/<角色>/interactions.json`
- 详细规则见 `doc/产品规划与路线图.md` §7「P3 落地规则」；P4 内容见本文档 §8

### 基础（早期阶段）
- 温馨小屋房间（墙体/地板/家具装饰）、网格 A* 寻路、家具碰撞、窗口自适应 / F11
- 角色：char_03 梅尔（左侧原图立绘 + 128×128 像素小人 0.25 缩放；`assets/chars/README.md` 为资源规范）
- 电脑 = 虚拟操作系统（ScreenArea 内不叠加；`Transition.*` + `show_desktop/open_game_select/open_gomoku/open_chat/open_diary/go_back/get_current_screen_id`）
- 游戏选择 + 15×15 AI 五子棋（胜 +3 / 平 +1 / 负 0）
- 聊天应用（OpenAI 兼容，`AIConnector.send_chat`）、日记应用（按日期自动整理；有 API 走 AI，失败降级规则）
- 记忆系统（`user://memory.json`：short_term 40 条 + long_term_summary ≤500 字 + key_facts ≤20 条）
- 好感度系统（`user://game.json`：0-100 默认 50；聊天 AI 判定 +1 每日上限 10）
- 房间布置（布置台 `decorate`：长按拾起/点击放下/恢复默认，存 `user://room_layout.json`；该交互不加好感）
- 新手攻略（首次进入自动弹，`general.guide_seen`）+ 右侧「家具介绍」（读 `FURNITURE_SPECS.desc`）

### P0 手机消息
- 入口：主界面「设置」图标下方手机图标（设置/手机均为图标按钮）
- `scenes/ui/phone_overlay.tscn` + `scripts/ui/phone_overlay.gd`：半透明竖屏单页聊天窗（不遮房间）；复用 `AIConnector.request_json` 返回 `{reply, emotion, action, affection, reason, mood_delta}`
- 消息节奏：发送 → `RECEIVE_DELAY=1.0s` → 提示音（`assets/sfx/notify.wav`）+ 头顶「！」（`notify_fx`）+ 顶部横幅（`notification_toast`）→ `REPLY_DELAY=0.8s` → AI 回复
- 系统提示不插入聊天流：手机右上角 `HintLabel` 逐条队列（滑入/停留 `HINT_DURATION=1.1s`/淡出）
- 角色反馈：气泡（`bubble.tscn`，36 字截断）+ `char_controller.play_action`（wave/hop/sad）
- 消息同样计入记忆/日记/好感度；AI 动作指令（家具交互 = 寻路后自动触发）

### P1 吸引移动
- 点击地板/家具 = 「引导」：点击处放置吸引物（`ATTRACT_ITEMS` 6 种：爱心/小鱼干/苹果/星星/毛线球/书卷；`general.attract_item` 默认 random，设置「通用」可固定或随机），物品上下漂浮后角色被吸引
- 反应：停顿 + 头顶「！」+ hop 小跳 → `ATTRACT_AFTER_DELAY=0.4s` → 自愿寻路；到达后物品消失
- 走神：好感度 <50 时 75% / ≥50 时 90% / ≥70 时 100% 响应；未响应时头顶「？」（与「！」同款特效，停留更久）
- 家具点击到达后自动触发交互（一次点击）
- 已移除原目标十字标记与寻路路径线（`_draw_target` / `_draw_path`）

### P2 自主行为 + 状态系统（初版）
- `scripts/autoload/StatusManager.gd`（新 autoload，`user://status.json`）：饱食/心情/疲惫 0-100（初始 70/60/30）内部 float 平滑变化；食物状态 `none|raw|cooked`；自主好感当日计数（每日上限 +3）；`last_seen` 时间戳
- 现实时间：在线缓慢变化（饱食 -1/30min、疲惫 +1/30min）；测试倍率 `general.dev_state_speed`（×1/×5/×10，设置→通用，**测试用，正式版移除**）
- 行为调度（`GameMain._autonomy_*`）：空闲 ≥8s 且电脑 OS/全屏浮层未开时开始（手机开着照常）；玩家任何输入打断；状态阈值优先（饿→吃 / 累→睡·沙发 / 心情低→窗边）→ 随机闲逛 → 现实钟点日程（22–6 倾向休息）；同目标冷却
- 进食链路（新家具）：冰箱 `fridge`（拿食物）→ 直接吃（生食 +20 饱食、+3 疲惫）或 厨具 `cook`（做饭，心情+1）→ 饭桌 `eat` 或 沙发 `rest` 吃（熟食 +35 饱食、+2 心情、+2 疲惫）；无食物时提示先拿
- 自主用电脑：走到电脑旁不弹 OS，电脑旁出现「一起玩」按钮（`UI/PlayTogetherButton`），点击 `open_panel()+open_game_select()` 直达游戏选择
- 自主好感：正式自主行为完成 50% 概率 +1，日上限 +3；自主行为写日记（`MemoryManager.record_daily_event(..., "autonomy", ...)`）
- 离线结算：上线时离线 ≥ `1800 / dev_state_speed` 秒 → 先按时间算自然变化，再 AI（带人设/记忆/状态摘要）输出 `{"satiety","fatigue","mood","affection","message"}` 一次性应用（±钳制）；`message` 显示为角色气泡并写日记；affection 计入每日 +3；失败自动降级为时间计算
- 聊天：`MemoryManager.build_chat_context` 注入好感度描述；手机/电脑判定支持 `mood_delta`（-2~+3 缺省 +1）经 `StatusManager.apply_delta` 应用
- 状态感知：纯表现（行为 + 偶尔气泡），无任何数值 UI

### P3 Graphwar 仿·函数对打（已实现，2026-08-30，多球版）
- `scenes/ui/graphwar_game.tscn` + `scripts/ui/graphwar_game.gd`：**双方各 3 个球**（玩家左侧蓝菱形、AI 右侧红圆，每轮 y 随机）；每回合点选自己一个球（可切换）直接输入函数表达式发射，**发射的球不会消耗、仍留在场上**，只有被对方命中才被击毁；炮弹从该球出发，沿「y = 球高度 + f(x) − f(0)」飞行（x = 离开出发点的水平距离）；**轨迹随炮弹移动逐步显示**；先击毁对方全部 3 球获胜
- 坐标范围扩大 5 倍：水平 ±50、垂直 ±25；场地有随机**球形障碍**（6~10 个，挡住炮弹并清除，不得分）；**炮弹命中球后不消失、可一次击毁多个球，仅碰到屏幕边缘或球形障碍才消失**；教程为黑底可滚动 RichTextLabel
- 表达式支持：+ - * / ^ ( )、数字、x、pi、sin/cos/tan/abs/sqrt/ln/exp，乘号可省略（0.5x）；无效公式状态栏提示
- AI：随机选自己的球 + 随机瞄对方剩余球，65% 概率解析瞄准（瞄准结果加随机偏移 ±1.2），35% 随机出招；函数命中按线段-圆求交判定
- 胜负沿用通用规则：胜 +3 / 平 +1 / 负 0；记录 `MemoryManager.record_daily_event(..., "graphwar", ...)`
- 已接入游戏选择界面（`game_select` → `computer_panel.open_graphwar`）；界面内附「教程」
- 已知简化：无 AI 难度档；障碍为一次性阻挡（不区分血量）；「8 张函数卡+滑杆」「单球 3 分制」旧方案均已废弃

### P3 立绘差分表情（已实现，2026-08-30）
- `scripts/ui/portrait_manager.gd`（挂在 `scenes/main.tscn` 的 `UI/PortraitLayer/PortraitTexture` 上）：按状态/事件切换左侧立绘
- 素材：`assets/chars/char_03/portraits/`（从 `D:\梅尔工作区\二创_被研究了！\02\素材\立绘\梅尔` 拷入 8 张 525×1043 原图）
- 6 档映射：默认=微笑 / 开心=超开心 / 低落=无奈闭眼 / 害羞=脸红微笑 / 走神=疑惑 / 惊讶=担心；备用档 angry=生气、blushing_worried=脸红担心（暂未接）
- 持久状态：心情 ≥80→开心；心情 <30 或饱食 <30→低落；其余默认（0.25s 检查 + `status_changed` 即时刷新）
- 事件临时表情（优先级：惊讶>害羞>生气>开心>低落>走神）：收到手机消息→惊讶 1.5s；手机情绪 happy/shy/sad→对应 2.5s；吸引走神（？）→走神 2.5s；看完电视/书→开心 8s

### P3 其他互动（角色文件数据驱动，已实现初版，2026-08-30）
- 新增 `assets/chars/char_03/interactions.json`：每个互动含 `name/entry/actions/expression/texts/effects/affection/limits`（每日次数/冷却）；加载器 `scripts/logic/character_interactions.gd`（`CharacterInteractions.load_specs(char_id)`）
- 触发：点击角色 → 互动菜单（摸摸头/投喂零食/玩小玩具，菜单按钮由角色文件生成）；书架=一起看书、盆栽=浇花（`entry:"furniture:read"` / `"furniture:water"` 匹配）
- 执行流程：上限检查（`GameManager.get/record_interaction_usage`，存 `user://game.json`）→ `StatusManager.apply_delta` → `GameManager.add_affection` → 日记 → `play_action` → 立绘表情 → 随机台词气泡
- 换角色/改互动只需编辑角色文件（`assets/chars/README.md` 已写规范）
- 已知简化：菜单为简易面板；投喂暂无库存系统；数值/频率待用户实测微调

### P3 井字棋（已实现，2026-08-30）
- `scenes/ui/tictactoe_game.tscn` + `scripts/ui/tictactoe_game.gd`：3×3 与 AI 对战（玩家 X / AI O，先三连者胜）
- AI 策略：能赢先赢 → 堵玩家 → 中心 → 随机角 → 任意（非完美博弈，玩家可赢）
- 胜负沿用通用规则：胜 +3 / 平 +1 / 负 0；记录 `record_daily_event(..., "tictactoe", ...)`；`GameManager.on_game_finished` 增加 `game` 参数（默认五子棋）
- 已接入游戏选择界面（`game_select` → `computer_panel.open_tictactoe`）

### P4 多角色切换（已实现，2026-09-04，P4 首项）

- 入口：主界面侧边栏「角色」图标（手机图标下方，新增 `assets/ui/icon_character.svg`）→ `scenes/ui/character_select_overlay.tscn` + `scripts/ui/character_select_overlay.gd`（立绘缩略图/名字/性格摘要/好感度列表；点击即切换，无二次确认；当前角色禁用并标「（当前）」）
- 角色目录自动发现：`scripts/logic/character_catalog.gd`（`CharacterCatalog`）扫描 `res://assets/chars/*`；可用判定=同时具备 `persona.json` + `sprites/`（idle_0/walk_0 之一）+ `portrait.png`；`portraits/` 差分立绘与 `interactions.json` 为可选，缺失时降级（立绘单张、无互动菜单）
- **单角色常驻**：切换=原地换人；小人动画（`char_controller.set_character`）、立绘（`portrait_manager.set_character`）、名字（`NameLabel`）、互动菜单（`_rebuild_interaction_menu`）、状态（`StatusManager.set_character`）全部按新角色加载
- **各角色数据完全独立**：好感度/记忆/日记/状态/互动次数按 char_id 分区；`GameManager` 新增 `get/set_current_char_id()` + `current_char_changed` 信号，选中角色持久化到 `user://game.json` 的 `current_char_id`；`StatusManager` 改为 `_char_id` 动态存取（`user://status.json` 各角色独立）
- 首批角色：char_03 梅尔（完整）+ char_01 小灯（基础素材自动入列，差分立绘/互动缺失降级）；`persona.json` 可选 `order` 控制列表排序（梅尔=1）；**char_01 小灯为临时占位角色**（仅基础素材），后续将替换为正式角色（素材待定）
- 动态文案：聊天/手机/日记/帮助/通知横幅/离线结算等「梅尔」硬编码已改为按当前角色显示名（`CharacterCatalog.get_display_name`）

### P4 21点（已实现，2026-09-04，P4 第二项）

- `scenes/ui/blackjack_game.tscn` + `scripts/ui/blackjack_game.gd`：与 AI 对战（AI = 当前角色担任庄家）
- 规则：A=1/11（自动适应不爆牌）、J/Q/K=10、超过 21 爆牌；玩家先要牌/停牌；停牌后 AI 翻暗牌并补牌到 ≥17；天然黑杰克（两张=21）自动开牌
- 胜负：点数大者胜，相等平局；沿用通用规则 胜 +3 / 平 +1 / 负 0；`MemoryManager.record_daily_event(..., "blackjack", ...)`
- 已接入游戏中心：`game_select`（21点按钮）→ `computer_panel.open_blackjack`；卡片为简单 `_draw` 绘制（♠♥♦♣ + 点数）

### P4 贪吃蛇（已实现，2026-09-04，P4 第三项）

- `scenes/ui/snake_game.tscn` + `scripts/ui/snake_game.gd`：与 AI 同场对战（玩家蓝蛇 / AI 红蛇，AI 名称取自当前角色）
- 场地 24×15 网格；方向键 / WASD 控制；同抢一个食物，吃到 +1 分并生长；**先吃满 8 分者胜**
- 判负：撞墙 / 撞自己 / 撞对方；头对头或同时撞 = 平局；撞上对方也可能双方死亡
- 胜负沿用通用规则 胜 +3 / 平 +1 / 负 0；`MemoryManager.record_daily_event(..., "snake", ...)`
- 已接入游戏中心：`game_select`（贪吃蛇按钮）→ `computer_panel.open_snake`

## 3. 未完成 / 已知问题

- P2 为初版，阈值/概率/时长等参数待用户实测后微调（集中在 `GameMain` 顶部常量与 `StatusManager` 常量）
- Graphwar 为简化版：无 AI 难度档；障碍为一次性阻挡（不区分血量）；后续可按需扩充
- 其他互动为初版（数据已可自定义；数值/频率待实测微调；投喂暂无库存）
- P4 待办：多方向行走动画、BGM/TTS、多房间/Mod/导出打包（多角色切换 + 21点 + 贪吃蛇已完成 2026-09-04）
- UI：游戏选择界面已修复（尾部按钮超屏/不可滑动 → `ScrollContainer` 滚动列表）；其余界面仍有手写绝对坐标、超屏隐患，**全局 UI 重构/重绘计划已记录** → `doc/UI重构与重绘计划.md`（待办）
- 立绘差分 6 档 ✅ 已实现（P3）；`angry.png`/`blushing_worried.png` 为备用档未接入
- 多方向行走动画（上下左右）：未做
- BGM 未做（已有手机提示音 `assets/sfx/notify.wav`）；TTS、流式输出：未做
- API Key 明文存 `user://config.json`；离线结算与聊天/日记均依赖真实 API，无 API 时按降级规则
- 房间布置为“长按-点击式”简化版；多房间、Mod、导出打包均未做

## 4. 关键文件

### 主流程
- `scenes/main.tscn`：主场景（房间/角色/立绘/侧边栏按钮/「一起玩」按钮）
- `scripts/GameMain.gd`：房间绘制、引导-吸引（P1）、进食链路（P2）、自主行为调度（`_autonomy_*`）、离线结算（`_run_offline_ai_settlement`）、手机/浮层接线（**主线，约 1600 行**）

### Autoload（`project.godot`）
- `GameManager`（好感度 `user://game.json`）
- `ConfigManager`（`user://config.json`：AI 配置、`general.guide_seen`、`general.attract_item`、`general.dev_state_speed`）
- `MemoryManager`（记忆/日记 `user://memory.json`）
- `AIConnector`（OpenAI 兼容；`request_json` 已做容错：去 Markdown 围栏 + 括号配对提取，失败打印原文到控制台）
- `StatusManager`（**P2 新增**：状态 `user://status.json`）

### P0 手机消息
- `scripts/ui/phone_overlay.gd` / `.tscn`：手机聊天窗（系统提示队列 `HintLabel`）
- `scripts/ui/bubble.gd` / `.tscn`：角色气泡
- `scripts/ui/notify_fx.gd` / `.tscn`：头顶「！」「？」特效
- `scripts/ui/notification_toast.gd` / `.tscn`：顶部通知横幅
- `assets/sfx/notify.wav`、`assets/ui/icon_phone.svg`、`assets/ui/icon_settings.svg`

### P3 立绘差分
- `scripts/ui/portrait_manager.gd`（挂主场景立绘节点）+ `assets/chars/char_03/portraits/`（8 张表情原图）

### P3 其他互动 / 井字棋
- `assets/chars/char_03/interactions.json` + `scripts/logic/character_interactions.gd`（角色文件数据驱动互动）
- `scripts/ui/tictactoe_game.gd` / `.tscn`（井字棋）
- `GameManager.get/record_interaction_usage`（互动每日次数/冷却，存 `user://game.json`）

### P4 多角色切换
- `scripts/logic/character_catalog.gd`（`CharacterCatalog`：角色扫描/可用判定/显示名/立绘路径）
- `scripts/ui/character_select_overlay.gd` / `.tscn`（角色选择浮层）+ `assets/ui/icon_character.svg`（「角色」按钮图标）
- 切换链路：`GameManager.get/set_current_char_id()` + `current_char_changed` 信号（持久化 `user://game.json`）→ `GameMain._apply_character` → `char_controller.set_character` / `portrait_manager.set_character` / `StatusManager.set_character` / `_rebuild_interaction_menu`

### P4 21点 / 贪吃蛇
- `scripts/ui/blackjack_game.gd` / `.tscn`（与 AI 对战；已接入 `computer_panel.open_blackjack` 与 `game_select`）
- `scripts/ui/snake_game.gd` / `.tscn`（与 AI 同场对战；已接入 `computer_panel.open_snake` 与 `game_select`）

### 电脑 OS / 应用 / 布置
- `scripts/ui/computer_panel.gd` / `.tscn`、`desktop_screen`、`game_select`、`gomoku_game`、`graphwar_game`（P3）、`tictactoe_game`（P3）、`blackjack_game`（P4）、`snake_game`（P4）、`chat_screen`、`diary_screen`
- `scripts/ui/settings_screen.gd` / `.tscn`（通用页含引导物品种类 + 状态变化速度）
- `scripts/ui/room_layout_overlay.gd` / `.tscn` + `room_layout_grid.gd`
- `scripts/ui/help_overlay.gd` / `.tscn`

### 家具规格
- `GameMain.FURNITURE_SPECS`（单一数据源：id/name/interaction/desc/size/pos/color；含 bed/desk/shelf/chair/tv/sofa/lamp/plant/layout/fridge/kitchen/dining）

### 文档
- `README.md`（展示 + 操作 + 当前功能 + 后续计划）
- `doc/产品规划与路线图.md`（**产品方向/分期/§7 已确认与 P2 落地规则，最重要**）
- `doc/开发文档.md`（架构/模块/验证）
- `doc/交互系统.md`、`doc/后续需求对齐.md`、`doc/UI重构与重绘计划.md`（P4 善后计划）、`doc/交接文档.md`（本文）

## 5. 运行与验证

```bash
# 冒烟测试（先挪开 user://config.json 防止离线结算/聊天触发真实 API 调用）：
# cd "$APPDATA/Godot/app_userdata/AIHome" && mv config.json config.json.bak
"C:/Users/JOYCEPC/Godot/Godot_v4.7.2-stable_win64_console.exe" --headless --path . --quit-after 150
# 测完恢复：cd "$APPDATA/Godot/app_userdata/AIHome" && mv config.json.bak config.json
```

- 新资源/新脚本后先跑一次 `--headless --import --path .` 再冒烟
- 编辑器中 F5 运行（自动创建 `user://status.json`）

## 6. 配置与运行数据

| 文件 | 内容 |
|---|---|
| `user://config.json` | AI 配置、`guide_seen`、`attract_item`、`dev_state_speed`（测试用） |
| `user://memory.json` | 记忆/日记 |
| `user://game.json` | 好感度 |
| `user://status.json` | P2 状态（饱食/心情/疲惫/食物/自主好感计数/last_seen） |
| `user://room_layout.json` | 家具布局 |

- `user://` = `%APPDATA%/Godot/app_userdata/AIHome`（仓库内 `Godot/app_userdata` 是旧副本，勿用）
- API Key 不要提交仓库；默认 Base URL `https://api.deepseek.com/v1`

## 7. 约定与教训

- 用户中文交流，偏好温馨暖色系；文档原则：README 面向展示（人话），需求/路线图进 `doc/`
- 需求对齐流程：用户口述 → 助手记录并追问 → 确认后写入文档（`doc/产品规划与路线图.md` §7）
- `AIConnector.request_*` 错误回调经 `bind` 会先收 message 参数导致错位；用 lambda 包装
- JSON 容错：`AIConnector._extract_json` 去围栏 + 括号配对提取外层；失败打印原文；prompt 用严格 JSON + 示例；`affection`/`mood_delta` 均做多形式归一化
- 冒烟测试前挪开 `user://config.json`（防真实 API 调用）
- 每游戏结束有结果提示 + 退出键 + 胜负/平局钩子

## 8. 下一步（P0–P3 已完成；P4 多角色切换 + 21点 + 贪吃蛇已完成 → 继续 P4 其余项）

- **交接结论（2026-08-30 → 2026-09-04 更新）**：P0–P3 已全部实现并提交；**P4 已完成多角色切换 + 21点 + 贪吃蛇**（2026-09-04），从本文档继续即可
- **本次完成（2026-09-04）**：① 多角色切换——主界面「角色」图标 → 角色选择列表 → 原地换人；梅尔 + 小灯（临时占位，后续替换正式角色）；角色自动发现；各角色好感/记忆/状态独立；差分立绘/互动缺失降级。② 21点——与 AI（当前角色=庄家）对战，接入游戏中心。③ 贪吃蛇——与 AI 同场对战（方向键/WASD），接入游戏中心。④ 游戏选择界面修复——按钮列表改为可滚动容器（不再超屏）。⑤ UI 重构/重绘计划已写入 `doc/UI重构与重绘计划.md`
- **P3 全部实现**：Graphwar 仿·函数对打（多球版）、井字棋、立绘差分（6 档）、其他互动（5 条，角色文件数据驱动）
- 下一阶段：**P4 其余** —— 多方向行走动画、BGM/TTS、多房间/Mod/导出打包（详情见 `doc/产品规划与路线图.md` §6/§7）
- 建议先实测：多角色切换表现（小灯为临时占位，后续替换）、21点手感、P2/P3 反馈（Graphwar 手感、立绘档位、互动数值），用于调参
- 其余需求/确认项见 `doc/产品规划与路线图.md` §7「待确认」
